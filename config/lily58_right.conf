# nice_nano's board defconfig sets CONFIG_ZMK_USB=y unconditionally, which is
# unsatisfiable on the peripheral half (depends on ZMK_SPLIT_ROLE_CENTRAL) and
# triggers a Kconfig warning. Assert the value it's forced to anyway.
CONFIG_ZMK_USB=n
